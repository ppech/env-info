@using global::EnvInfo.Core
@inject EnvInfoOptions envInfoOptions

@if (envInfoOptions.Visible)
{
    <div class="env-info">
        <div class="content">
            @ChildContent
        </div>
    </div>
}

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; } = default!;
}